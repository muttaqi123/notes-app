# Where the API lives. Leave it empty in development: the Vite dev server
# proxies /api to http://localhost:4000, so no CORS and no absolute URLs.
# In production set it to the deployed API origin, e.g.
# VITE_API_URL=https://keep-notes-api.onrender.com
VITE_API_URL=
